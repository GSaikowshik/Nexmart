# Render Blueprint configuration for NexMart FastAPI backend
services:
  - type: web
    name: nexmart-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
